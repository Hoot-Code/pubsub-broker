module github.com/Hoot-Code/pubsub-broker

go 1.22

require (
	// Zero external dependencies — stdlib only.
	// To add YAML config support: go get gopkg.in/yaml.v3
	// Config currently uses JSON (encoding/json) for zero-dep builds.
)
